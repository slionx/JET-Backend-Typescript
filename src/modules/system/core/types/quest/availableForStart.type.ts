export type TquestAvailableForStart = {
    _parent: string,
    _props: {
        compareMethod: string,
        value: string,
        index: number,
        parentId: string,
        id: string
    }
}